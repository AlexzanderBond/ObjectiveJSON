/**
 * One might notice that JSON isn't capitalized in the names of classes and probably methods, this is because it is easier to separate words when the have lowercase letters following uppercase ones
 */
module ObjectiveJSON {
    requires org.jetbrains.annotations;
    exports edu.cbet.json;
    exports edu.cbet.json.annotations;
}